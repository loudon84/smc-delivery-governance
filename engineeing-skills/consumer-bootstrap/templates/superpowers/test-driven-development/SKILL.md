---
name: test-driven-development
description: >-
  GES_NATIVE method shim until upstream Superpowers bytes are installed locally.
  RED/GREEN evidence belongs to GES Engineering Method v2. Claim: GES_NATIVE.
---

# Test-Driven Development (GES_NATIVE shim)

Follow GES Engineering Method v2: focused RED before GREEN, epoch rules, and
Delivery ownership via `smc-plan-delivery`. Prefer upstream-pinned skill when
managed overlay provides it.

Claim state: `GES_NATIVE` (not `UPSTREAM_PINNED` unless provenance-backed bytes exist).
