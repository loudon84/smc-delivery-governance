---
name: systematic-debugging
description: >-
  GES_NATIVE method shim until upstream Superpowers bytes are installed locally.
  Delivery ownership remains with smc-plan-delivery. Claim: GES_NATIVE.
---

# Systematic Debugging (GES_NATIVE shim)

Use structured root-cause analysis before claiming fixes. Record debug events under
GES Delivery / engineering method gates. Prefer upstream-pinned skill when present
in `.agents/skills/systematic-debugging/` from a managed overlay.

Claim state: `GES_NATIVE` (not `UPSTREAM_PINNED` unless provenance-backed bytes exist).
