# Complexity routing

SPIKE is research without retained production changes. BOUNDED requires one known existing owner, bounded writes and deterministic verification with no architectural risks. Governed BOUNDED uses LEAN; architecture-impacting or unknown work uses FULL. NONE -> LEAN -> FULL is monotonic for the same work item.
