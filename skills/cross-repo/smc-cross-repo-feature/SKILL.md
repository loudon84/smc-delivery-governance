---
name: smc-cross-repo-feature
version: 1.0.0
description: 创建和维护跨仓 Feature SOT，冻结参与仓库、Owner、Global Change ID、Contract 与 Integration Outcome。
---
# SMC Cross Repo Feature

输出 `features/FEAT-*/feature.yaml + architecture.md + roadmap.yaml + dependencies.yaml + work-packages/`。

规则：先注册 Repository/Project；一个 Global Capability 一个 Owner；Producer/Consumer 明确；Global Change 使用 `XR-Cnn`；每个参与 Repo 至少一个 Work Package；Integration Owner 必须明确。
