# Harness Projection

Shared skills land in `.agents/skills/`. Cursor and Codex only receive GES-managed integration files. AGENTS.md is a SECTION artifact hashed on marker bytes only.

Same path plus different content is `PROJECTION_PATH_CONFLICT`. Spec Kit wrappers cite `.specify/.ges/commands/<cap>.md` plus pinned repo/SHA/path. Marker upsert is [[ges/harness_adapters/agents_md.py#apply_marker]].
