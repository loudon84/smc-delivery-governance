# Harness Projection

Shared skills land in `.agents/skills/`. Cursor and Codex only receive GES-managed integration files. AGENTS.md is marker-edited, never overwritten.

Marker upsert and user-edit detection are in [[ges/harness_adapters/agents_md.py#apply_marker]]. Hermes remains an interface that raises `HARNESS_NOT_SUPPORTED`.
