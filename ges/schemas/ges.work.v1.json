{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ges.work.v1",
  "title": "GES Work Declaration",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "id", "kind", "title", "status", "owner", "risk", "policy", "artifacts", "created_at", "updated_at"],
  "properties": {
    "schema": { "const": "ges.work.v1" },
    "id": { "type": "string", "pattern": "^[A-Z][A-Z0-9._-]{2,63}$" },
    "kind": { "const": "FEATURE" },
    "title": { "type": "string", "minLength": 1 },
    "status": { "type": "string", "enum": ["OPEN", "CLOSED"] },
    "owner": { "type": "string", "minLength": 1 },
    "risk": { "type": "string", "enum": ["LOW", "MEDIUM", "HIGH"] },
    "policy": { "const": "default-v1" },
    "artifacts": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "type", "pointer", "digest"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "type": { "type": "string", "enum": ["SPEC", "PLAN"] },
          "pointer": { "type": "string", "pattern": "^repo://" },
          "digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" }
        }
      }
    },
    "created_at": { "type": "string", "minLength": 1 },
    "updated_at": { "type": "string", "minLength": 1 }
  }
}
